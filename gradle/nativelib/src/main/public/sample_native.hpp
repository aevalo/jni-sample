#ifndef SAMPLE_NATIVE_INCLUDED
#define SAMPLE_NATIVE_INCLUDED

void uppercase(char* str);
int square(int num);
bool invert(bool boolean);
int array_sum(int array[], int len);

#endif // SAMPLE_NATIVE_INCLUDED
